# Environment variables (never commit real values)
.env

# Python virtual environments
.venv/
venv/

# Python cache
__pycache__/
*.pyc

# Pytest cache
.pytest_cache/

# Supabase CLI local cache (project ref, versions, pooler URL — regenerated by `supabase link`)
supabase/.temp/

# Playwright persistent browser profile / artifacts (Phase 4B)
.runtime/
browser_profiles/
playwright-report/
test-results/

# OS junk
.DS_Store
