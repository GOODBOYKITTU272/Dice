# ── Supabase client (repository layer, RPC calls) ─────────────────────────────
supabase

# ── Environment variables (.env support) ───────────────────────────────────────
python-dotenv

# ── Testing ─────────────────────────────────────────────────────────────────────
pytest

# ── Dice discovery: HTTP + HTML parsing (Phase 2, no browser needed) ──────────
requests
beautifulsoup4

# ── Local operator UI (Phase 2) ────────────────────────────────────────────────
flask

# ── Persistent Dice browser foundation (Phase 4B) ──────────────────────────────
# Pinned to the latest stable at the time this was added. After installing,
# also run `playwright install chromium` once (downloads the browser binary
# separately — not part of the pip package).
playwright==1.62.0

# ── Upstream Dice parsing helpers (Phase 3A) ───────────────────────────────────
# Pinned deliberately, not left open-ended: no LICENSE file upstream, and we're
# depending on undocumented behavior (Dice isn't even mentioned in their
# README) that could change without a visible signal in a future release.
# Only search/parsing utilities are used — never Dice.scrape() or
# Dice._fetch_job_details() wholesale (see dice/upstream_adapter.py for why).
jobspy-enhanced-scraper==1.3.7
